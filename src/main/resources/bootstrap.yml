spring:
  application:
    name: config-client
  profiles:
    active: development
  cloud:
    config:
      uri: "http://localhost:8092"

management:
  endpoints:
    web:
      exposure:
        include: refresh